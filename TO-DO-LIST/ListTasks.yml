info:
  name: ListTasks
  type: http
  seq: 2

http:
  method: GET
  url: http://127.0.0.1:8000/api/tasks/list
  body:
    type: json
    data: |-
      {
        "title": "Сделать тестовое задание",
        "description": "Создать API задач",
        "status": "done"
      }
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
